# Quickstart: проверка задачи закрытия портов

1. Запустить `START_MVP_SPHERE_SR.py` установленным Python 3.11+.
2. Загрузить XLSX credentials, отдельный XLSX «Список устройств для закрытия портов 80 и 23 (http, telnet)» и выбрать папку результатов; основная SR для этой задачи не требуется.
3. Выбрать точный Huawei TE40 по IP либо фильтрами из отдельного списка.
4. Установить «Закрыть порты 80, 23 (http, telnet)» и подтвердить число целей.
5. После сохранения JSON проверить `managementActions[0]`.

Успешны `applied` и `already_compliant`, если `after` содержит `telnetPort23=disabled` и `httpPort80=disabled`, полученные повторным чтением меню кодека. Состояние TCP listener не проверяется. Любой `failed` требует ручной проверки.
